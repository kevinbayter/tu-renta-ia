# Secretos — nunca commitear
.env
.env.local
.env.*.local

# Dependencias y builds
node_modules/
dist/
build/
.next/
.turbo/
coverage/

# Sistema
.DS_Store
*.log

# Documentos personales del caso dorado (contienen PII real)
docs/

# Artefactos de pruebas de navegador
.playwright-mcp/

# Prisma client generado (se regenera con pnpm --filter @turenta/adaptadores db:generate)
packages/adaptadores/src/persistencia/generado/
